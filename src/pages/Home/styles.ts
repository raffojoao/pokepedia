import styled from 'styled-components/native';

export const Test = styled.View`
  flex: 1;
  background-color: ${({theme}) => theme.colors.types.fairy};
  align-items: center;
  justify-content: center;
`;
